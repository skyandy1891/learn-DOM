const titleEl = document.querySelector(".title");
// добавляем ссылку
// titleEl.innerHTML = "<a href=''>Это ссылка :)</a>";

// полностью очищает всё внутри данного элемента
// titleEl.innerHTML = "";

// console.log(titleEl.innerHTML);

// insertAdjacentHTML добавляет элемент не меняя внутри него ничего

// добавляет элемент ПЕРЕД НАЧАЛОМ
// titleEl.insertAdjacentHTML("beforebegin", "<a href=''>Это ссылка :)</a>");

// добавляет элемент ПОСЛЕ, т.е. следующим соседом
// titleEl.insertAdjacentHTML("afterend", "<a href=''>Это ссылка :)</a>");

// добавит элемент ПЕРВЫМ ВНУТРИ
// titleEl.insertAdjacentHTML("afterbegin", "<a href=''>Это ссылка :)</a>");

// добавит элемент ПОСЛЕДНИМ ВНУТРИ

// titleEl.insertAdjacentHTML("beforeend", "<a href=''>Это ссылка :)</a>");
